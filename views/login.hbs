<div class="headline">
    {{#if success_msg}}
        {{ success_msg }}
    {{else}}
        Logga In
    {{/if}}
</div>
<div class="login-form">
    {{#if error}}
        <p class="errorMsg">{{ error }}</p>
    {{/if}}
    <form action="/users/login" method="post">
        <input type="text" name="email" class="form-input" placeholder="E-mail" required>
        <br>
        <input type="password" name="password" class="form-input" placeholder="Lösenord" required>
        <br>
        <button type="submit" class="form-submit">Logga in</button>
    </form>
</div>